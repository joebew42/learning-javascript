import { assert } from "chai";

describe("SalarySlipGenerator", () => {
  beforeEach(() => {});

  it("test", () => {
    let actual = true;
    let expected = true;

    assert.equal(actual, expected);
  });
});
