var assert = require('chai').assert

describe("SalarySlipGenerator", () => {
  beforeEach(() => { });

  it("test", () => {
    let actual = true
    let expected = true

    assert.equal(actual, expected)
  });
});
